console.log(`------------------------------------------------`);
var index = 5;
 while(index <=15) {
    console.log( index);
    index++;
    }
    
    console.log(`------------------------------------------------`);
var index = 50;
 while(index >=40) {
    console.log( index);
    index--;
    }
    
    console.log(`------------------------------------------------`);
var index = 1;
 while(index <=30) {
    console.log( index);
    index=index+2;
    }
    
    console.log(`------------------------------------------------`);
var index = 0;
 while(index <=20) {
    console.log( index);
    index=index+2;
    }
    
    console.log(`------------------------------------------------`);
var index = 5;
 while(index <=50) {
    console.log( index);
    index=index+5;
    }
    
    console.log(`------------------------------------------------`);
var index = 10;
 while(index <=100) {
    console.log( index);
    index=index+10;
    }

    console.log(`------------------------------------------------`);
var index = 100;
 while(index >=10) {
    console.log( index);
    index=index-10;
    }
    

    
    